// generated by s3.genlib.py
#pragma once
#include <functional>
#include <tuple>
#include <map>
#include <string>

using namespace std;
typedef function<int(int, int)> tOperation;
typedef tuple<tOperation, tOperation, tOperation, tOperation, tOperation> tOp5;
typedef tuple<tOperation, tOperation, tOperation, tOperation, tOperation, tOperation, tOperation, tOperation, tOperation, tOperation, tOperation> tOp11;

class AxLibrary {
    map<string, tOp11> ops;
public:
    AxLibrary();

    tOp11 GetFunction(string name);
};
